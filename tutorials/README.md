# Tutorials

[![GitHub](https://img.shields.io/badge/GitHub-181717?style=flat&logo=github&logoColor=white)](https://github.com/kurtvalcorza/qwen3-reranker-pipeline)
[![Open In Colab](https://colab.research.google.com/assets/colab-badge.svg)](https://colab.research.google.com/github/kurtvalcorza/qwen3-reranker-pipeline/blob/main/tutorials/qwen3_reranker_colab.ipynb)
[![Hugging Face](https://img.shields.io/badge/%F0%9F%A4%97%20Hugging%20Face-Qwen%2FQwen3--Reranker--0.6B-ffcc4d?style=flat)](https://huggingface.co/Qwen/Qwen3-Reranker-0.6B)
[![Upstream](https://img.shields.io/badge/Upstream-QwenLM%2FQwen3--Embedding-181717?style=flat&logo=github&logoColor=white)](https://github.com/QwenLM/Qwen3-Embedding)
[![arXiv](https://img.shields.io/badge/arXiv-2506.05176-b31b1b.svg)](https://arxiv.org/abs/2506.05176)

Notebook specification: **DIMER Notebook Specification 2.0** — the notebook is **standalone** (§4) and declares its profile and pedagogical mode (§3.4): it is
generated by `tools/build_notebook.py` from `tools/notebook_template.py` and carries the repository's
pipeline module, model identity, snapshot manifest and runtime pins, so the exported `.ipynb` keeps working
without this repository being reachable. Do not edit the notebook by hand; edit the package or the template
and regenerate (`python tools/build_notebook.py`; `--check` is enforced by the validator and CI).

| Notebook | Profile | Mode | Carrier | Capability | Default runtime | Sample | BYOD | Run-all | Release status |
|---|---|---|---|---|---|---|---|---|---|
| `qwen3_reranker_colab.ipynb` | `TASK-INFERENCE` | `GUIDED` | standalone (generated) | Qwen3-Reranker-0.6B pointwise query-document reranking on a synthetic query with three authored candidates; `yes`-share softmax relevance scores (not calibrated, no threshold) and a stable ranking; no metric exists or is reported; `validate_inputs` → input manifest; `evaluation_report` → always `not-measurable` (no ranking-metric helper, no judgements) | CPU float32 (CUDA bfloat16 used automatically when available) | automatic | one UTF-8 text file (query line + document lines), gated off by default | unverified — no clean-runtime `Run all` execution recorded for this revision (REL1/REL8) | **Candidate** — static checks pass; the clean-runtime execution row in `../docs/release-verification.md` is pending and must be recorded for the exact notebook revision before promotion |

## Conformance notes

_Requirement identifiers in these notes (for example `ST1`, `PAR3`, `DAT24`, `EVAL21`) are the NOTEBOOK_SPEC 1.1 numbering they were written against; NOTEBOOK_SPEC 2.0 renumbered its sections and the pull request that migrated this repository reports conformance against the 2.0 pre-flight checklist (§26) and report format (§30)._

- **Standalone carrier (ST1–ST6):** the primary path performs no clone, repository install, or repository import. Section 2 is `src/qwen3_reranker_pipeline/pipeline.py` verbatim (tagged `metadata.dimer.embedded_module`; the only rewrite makes the default weights directory working-directory-relative), Section 3 carries the model identity and the 13-file SHA-256 manifest inline and asserts they agree with the module before anything is fetched, and Section 1 carries the exact `pyproject.toml` runtime pins. `metadata.dimer.generated_from` records the repository, the revision generated from, the module's SHA-256, and the generator version.
- **Parity (PAR1–PAR4):** `tests/test_notebook_parity.py` and `tools/validate_release_assets.py` fail when the carried module, the inline manifest, or the inline pins differ from the repository, or when the notebook is not byte-identical to the generator's output.
- **Role stages:** `validate_inputs` (DAT24) writes `outputs/qwen3_reranker_input_manifest.json` with the schema, ceilings, per-pair character counts, the instruction in force, the verdict and one recorded rejection finding; `evaluation_report` (EVAL21) writes `outputs/qwen3_reranker_evaluation_report.json`, whose verdict is always `not-measurable` because the repository ships no ranking-metric helper — the report names the per-query relevance judgements and caller-side nDCG/MRR code that would make the task measurable (EVAL9).
- The notebook exercises `Qwen3RerankerPipeline` carried verbatim in the notebook rather than reimplementing model loading; the pipeline pins the immutable upstream revision, stages the missing snapshot file through the package's `stage_missing_files(..., allow_download=True)`, loads only from a digest-verified local snapshot (`verify_snapshot`), and refuses remote model code. The notebook never calls `transformers` or `huggingface_hub` directly and never reaches text generation.
- Score semantics (UNC1/UNC2/UNC4): the score is the `yes` share of a two-way softmax over the last-position `yes`/`no` logits — a relevance score, not a calibrated probability; `ranking` is an ordering, not a decision; the pipeline ships no threshold and the caller owns any cut-off. The `instruction` is surfaced as part of the prompt.
- No intrinsic metric exists (EVAL9): the repository ships no metric helper; the notebook says so, names what a real evaluation needs (relevance judgements per query-candidate pair and the caller's own nDCG/MRR code over many queries), and presents the synthetic ordering as a falsifiable plumbing check only. Recorded `SHOULD` deviation: EVAL11 (no baseline — none is meaningful without judgements).
- Ceilings `MAX_PAIRS`, `MAX_TEXT_CHARS`, `MAX_TEXT_TOKENS` are surfaced before the model runs; per-pair `n_tokens` and `truncated` flags are printed and exported after it (DAT22/DAT23).
- The default sample is synthetic text authored in code; `USE_BYOD` defaults to `False` so the sample path never opens an upload dialog.
- `tools/validate_release_assets.py` performs source validation only. It does not satisfy the
  clean-runtime execution requirement; a release review must confirm that a recorded clean run in
  `docs/release-verification.md` matches the notebook revision under review before the status is
  promoted to `Release-grade`.
